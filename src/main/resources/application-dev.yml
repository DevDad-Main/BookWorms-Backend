spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        forma_fql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 1025
    username: Oliver
    password: oliver
    properties:
      mail:
        smtp:
          trust: "*" # development purposes
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
application:
  security:
    jwt:
      # Generated using - openssl rand -base64 32
      secret-key: hLItECuTcQQpkpZMpfzVmFemDYDFA6Rg1RWSSuuEdHI=
      expiration: 8640000
  mailing:
    frontend:
      activation-url: http://localhost:4200/activate-account
server:
  port: 8088
